#include "const.h"
#include "../sdl2-light.h"
struct textures_s
{
    SDL_Texture *background; /*!< Texture liée à l'image du fond de l'écran. */
    SDL_Texture *enemy;
    SDL_Texture *ship;
    SDL_Texture *missile;
};
